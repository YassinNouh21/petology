import 'package:flutter/material.dart';

class AdaptationInfoPage extends StatelessWidget {
  const AdaptationInfoPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('AdaptationInfo'),
    );
  }
}